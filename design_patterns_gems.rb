gem 'active_decorator' # A simple and Rubyish view helper for Rails
gem 'draper' # Draper adds an object-oriented layer of presentation logic to your Rails apps.
gem 'view_component' # View components for Rails
gem 'mutations' # Compose your business logic into commands that sanitize and validate input.
gem 'params_ready' # Create well defined controller interfaces. Sanitize, coerce and constrain incoming parameters to safely populate data models, hold session state in URI variables across different locations, build SQL queries, apply ordering and offset/keyset pagination.
gem 'dry-schema' # dry-schema provides a DSL for defining schemas with keys and rules that should be applied to values. It supports coercion, input sanitization, custom types and localized error messages (with or without I18n gem). It's also used as the schema engine in dry-validation.